{
  "name": "github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node test/validate-html.js",
    "generate-video-sitemap": "node scripts/generateVideoSitemap.js",
    "update-robots": "node scripts/updateRobots.js",
    "ping-indexnow": "node scripts/pingIndexNow.js",
    "video-sitemap": "npm run generate-video-sitemap && npm run update-robots && npm run ping-indexnow",
    "build": "npx clean-css-cli -o styles.min.css styles.css && npx terser script.js -c -m -o script.min.js",
    "audit": "npx lighthouse http://localhost:8080/index.html --quiet --chrome-flags='--headless --no-sandbox' --output=json --output-path=lh-report.json && npx @axe-core/cli http://localhost:8080/index.html --save axe-report.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "htmlhint": "^1.6.3",
    "jsdom": "^26.1.0"
  },
  "dependencies": {
    "xmlbuilder2": "^3.1.1"
  }
}
